name: PPS Cleanup

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  cleanup:
    uses: ./.github/workflows/commit-push.yml
    with:
      module: "pps"
      scope: "cleanup"
    secrets:
      PAT: ${{ secrets.PAT }}
